이 버전은 register_globals=off 버전에서 동작하도록 수정된 게시판입니다.

해결 방법은 매우 다양하게 있으나 가독성을 위해서 일부러 간단한 방법을 사용하였습니다.

$no, $id 는 모두 GET 방식으로 넘어오는 값입니다. 

따라서 $_GET[id] 와 같은 방법으로 받으셔야 하고

글쓰기 등에서 사용하는 title, name, pass 같은 값은 모두 POST 방식의 값입니다.

따라서 $_POST[title]과 같은 방법으로 값을 읽으시면 됩니다.
